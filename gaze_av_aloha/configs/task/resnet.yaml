defaults:
  - base_gaze_policy_config

use_gaze: false

vision_encoder: resnet
vision_encoder_kwargs:
  crop_shape: [216, 288]
  pool: true

optimizer_lr: 1e-4
optimizer_lr_backbone: 1e-4

image_to_gaze_key:
  observation.images.left_eye_cam: left_eye